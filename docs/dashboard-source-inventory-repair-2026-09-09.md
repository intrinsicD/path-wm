# Large source-list presentation repair

The overnight decoder diagnostic completed all42conditions, but its source list
exceeds the portable reader's4000-character table-cell limit. A subsequent
geometry development unit therefore also failed publication despite passing its
numerical control. Preserve both logs and raw results. Do not rerun training or
raise the reader's limit.

Replace comma-joined source cells with a numeric source count and a separate exact
source table, one record-key/path pair per row. Produce both directly from the
existing SQLite JSON source records; do not split a string on commas. Retain every
source path, including paths with commas, and join stable keys to run identities.
Existing table partitioning handles larger inventories. Add one essential source
preservation/identity test and observe its informative failure first. Then refresh
and browser-verify the canonical dashboard, record publication-only repairs, commit
the completed slice and resume the remaining geometry development unit.

The repaired snapshot is now close to the unchanged3MB portable payload cap.
Reserve room for the remaining exact measurements by losslessly encoding embedded
scientific PNG previews as WebP when smaller. Original PNGs and source hashes stay
unchanged. This is a file-encoding change, with no resizing, color quantization or
pixel editing. Decode every encoded preview and verify exact RGBA pixel equality
before embedding; keep PNG when it is smaller. Add an essential round-trip check,
including colored fully transparent pixels, before implementation. Verify both
the dashboard and report in the actual browser; preserve the original figures for
scientific export. Do not relax payload or validation limits.
