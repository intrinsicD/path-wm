# Architecture discussion and validation checklist

Red = a dedicated discussion remains. Blue = discussed, with validation still incomplete. Green = validated within the explicitly labelled test scope, not general capability. Discussion coverage is retained separately, including for green parts that still need a walkthrough. Evidence refers to the recorded configurations and must be revisited after relevant changes.

Last updated: 2026-09-16. [Colored overview](architecture-atlas.html#01-overview).

The user requested red for parts still to discuss and green for validated parts. After substantive discussion or validation, update the matching coverage and evidence in `docs/diagrams/architecture-atlas.json`, then regenerate the atlas. Green requires a stated scope, limits and passing evidence for that scope; an overall failed experiment can support only an independently passing subcheck. Remove or revise green when that evidence no longer applies. An assistant-only explanation does not automatically complete a discussion. User corrections override the discussion assessment.

| Part | Discussion coverage | Conversation basis | Remaining walkthrough / follow-up |
| --- | --- | --- | --- |
| World / user / environment | Discussed | Discussed webcam footage, real versus synthetic data, and desired image/video/audio/text interaction. | Revisit concrete sensor and environment choices when integrating them. |
| Observation adapters | To discuss | Desired inputs were discussed; the observation-adapter/event contract has only appeared in the architecture map. | Walk through timestamps, modality synchronization, missing/late packets and event commits. |
| Modality encoders → §2–3 | Discussed | Discussed modality stems, residual transformer blocks per scale, hierarchy merges, fusion, temporal handling and reconstruction probes. On 14 September, the user asked whether architectural structure and a small parameter count can reduce visual training-data requirements. On 15 September, the user specified a spatial VAE with separate lossless rearrangement, residual processing and channel projection; Claude review was reconciled. | Opt-in spatial VAE v2 now implements the R/P/M/C stem, coarsest self-attention/shared loop and detached stage probes. Mechanics, exact GPU resume and ten real-photo learning sanity gates pass; all photo-quality screens fail. See docs/spatial-vae-v2-plan.md and runs/spatial_vae_v2/verification.json. Color diagnosis now finds reduced readout access at the final posterior projection and sampling; see docs/spatial-vae-color-plan.md. Current agent integration, semantic retention, fine detail and matched-resource/data-efficiency studies remain open; the high-level encoder stays blue. Two-seed sampled-color supervision now improves global chroma error81–83% without new parameters/inference work; achieved KL rises37–41% and grids remain. See docs/spatial-vae-repair-plan.md. Color-only is a diagnostic candidate, not a full repair; high-level status stays blue. All four modality interfaces now have isolated and mixed packet/mask/gradient/restart checks. Four-word and four-tone direct fitting pass; a video foreground repair fails total RGB quality. State-conditioned adapters and general multimodal semantics remain untrained in this audit. See docs/modality-foundation-plan.md; high-level status stays blue. |
| Predict and correct → §4 | To discuss | Discussed latent world/entity beliefs conceptually, but not the actual categorical predict/correct mechanism with h, codes and logits. | Walk through one observation: prior prediction, posterior correction, uncertainty and recurrent state. The source-grounded latent-core drawing distinguishes dynamical h updates from categorical observation correction; a dedicated user walkthrough remains pending. Alex asks why direction fails. Read-only diagnostics find69–90% known-direction linear access in raw-aux posterior probabilities versus50–54% in sampled codes; the trained pre-sampling reader also fails. Code collisions are measured, but no unique causal failure point is established. See runs/modality_repair_v1/direction_localization/report.html. Dedicated full predict/correct walkthrough remains open. Literature review: docs/direction-literature-review.md compares categorical estimators, paired supervision and query pooling. VQ warm-up transfer is tentative; the current classification task has no KL loss. No architecture or validation promotion. 16 September: eight fresh-updater controls (direction-only/joint, sampled/continuous working readout) all fail the task screens. Frozen encoders retain100% known/held-out factor access, but updater query direction access is60–75%. No unique causal loss layer or default repair established. Next isolate one input modality before mixing modes. See docs/modality-readout-plan.md and runs/direction_learning_v1/report.html. Colors and general capability scopes stay unchanged. |
| Session memory + optional World State → §5/13 | Discussed | Discussed recent inputs, hierarchical history, external entity storage, retrieval, latent versus raw evidence and provenance. On 15 September, reviewed the user-proposed multimodal World State: stable IDs, learned components/relations, evidence, reversible attribution and optional spatial representations. See docs/world-state-proposal-review.md. | The requested modular foundation is now implemented with versioned components, provenance, atomic session commits, revocable aliases, dependent-state invalidation/replay and bounded retrieval. See docs/world-state.md. Next: real candidate/identity/state tests, learned retrieval, abstention calibration and controlled comparisons. General concept induction remains a separate capability milestone. |
| Task workspace → §6 | Discussed | Discussed focus on entities and relationships, recalled context, internal thinking and interleaving internal/external actions. Reviewed separate binding-candidate and task retrieval, with bounded evidence/context and an explicit unresolved result. On15 September the user clarifies that thinking stays in a shared multimodal latent space. Each decoder should learn to extract what its output needs, with an optional modality-specific adapter; a shared text-shaped answer plan is not required. The user also permits nested Loop Transformers for shared thinking and modality-specific extraction. | The optional World State now feeds bounded component/relation tokens into the actual thinker, with replaceable query/context modules and shared training/runtime projections. A tiny paired-history task works. Compare oracle, flat episodic, structured and no-history conditions on held-out tasks; general learned retrieval and stopping remain open. See docs/latent-core.md and atlas14-15: shared multimodal Thinker and existing decoder attention, with optional modality-specific readout adapters. Compare current reads against a small adapter; workspace-only conditioning remains an optional diagnostic, not a required design. Optional recurrent adapters and requested-time video decoding are now implemented. Two-seed separate/joint readout testing fails all192 combined-output screens; no recurrent variant is adopted. Oracle tone output passes, while text recombination and native image/video remain weak. See docs/modality-readout-plan.md. Diagnose core retention and output conditioning separately; general capability remains open. Follow-up: eight longer native oracle fits now pass known-output gates, but new text/image combinations and video quality still fail. Training-only raw-logit supervision improves known position to97–100%, not direction; full core gates fail. Frozen probability-to-code probes and measured direction collisions motivate paired updater learning. See docs/modality-readout-plan.md and runs/modality_repair_v1/report.html. No default or green capability promotion. Literature review: docs/direction-literature-review.md compares categorical estimators, paired supervision and query pooling. VQ warm-up transfer is tentative; the current classification task has no KL loss. No architecture or validation promotion. 16 September: eight fresh-updater controls (direction-only/joint, sampled/continuous working readout) all fail the task screens. Frozen encoders retain100% known/held-out factor access, but updater query direction access is60–75%. No unique causal loss layer or default repair established. Next isolate one input modality before mixing modes. See docs/modality-readout-plan.md and runs/direction_learning_v1/report.html. Colors and general capability scopes stay unchanged. |
| Task request | To discuss | Discussed broad goals, complex tasks and decomposition; the instruction-to-task contract has not had a dedicated walkthrough. | Define task tokens, output controls, completion criteria and who verifies success. |
| Optional bounded planner → §11 | Discussed | Discussed action DAGs, predicted states, goal reachability, subgoals, new observations and replanning. | General DAG construction and learned search remain proposed, even though the topic has been discussed. |
| Modality outputs → §7 | Discussed | Discussed every encoder/decoder pair, state-conditioned outputs, image detail, conditional generation and replacement interfaces. On 15 September, discussed a mirrored PixelShuffle VAE decoder, Gaussian loss/KL units and a future state-conditioned spatial-latent producer. On15 September the user clarifies that thinking stays in a shared multimodal latent space. Each decoder should learn to extract what its output needs, with an optional modality-specific adapter; a shared text-shaped answer plan is not required. The user also permits nested Loop Transformers for shared thinking and modality-specific extraction. | Separate v1/v2 VAE decoders support exact saved-latent replay and variable geometry; v2 has a latent-only convolution/PixelShuffle decoder with final RGB projection. Ten photo fits still fail the quality screen; rates and common-target stage probes are measured in docs/spatial-vae-v2-plan.md. Constant latent controls locate decoder grids at learned subpixel phase channels; mean-only decoder fitting improves color but fails sampled-output reliability. See docs/spatial-vae-color-plan.md. State-conditioned generation, fine detail and other modalities remain open; this high-level output node stays blue. Two-seed sampled-color supervision now improves global chroma error81–83% without new parameters/inference work; achieved KL rises37–41% and grids remain. See docs/spatial-vae-repair-plan.md. Color-only is a diagnostic candidate, not a full repair; high-level status stays blue. All four modality interfaces now have isolated and mixed packet/mask/gradient/restart checks. Four-word and four-tone direct fitting pass; a video foreground repair fails total RGB quality. State-conditioned adapters and general multimodal semantics remain untrained in this audit. See docs/modality-foundation-plan.md; high-level status stays blue. See docs/latent-core.md and atlas14-15: shared multimodal Thinker and existing decoder attention, with optional modality-specific readout adapters. Compare current reads against a small adapter; workspace-only conditioning remains an optional diagnostic, not a required design. Optional recurrent adapters and requested-time video decoding are now implemented. Two-seed separate/joint readout testing fails all192 combined-output screens; no recurrent variant is adopted. Oracle tone output passes, while text recombination and native image/video remain weak. See docs/modality-readout-plan.md. Diagnose core retention and output conditioning separately; general capability remains open. Follow-up: eight longer native oracle fits now pass known-output gates, but new text/image combinations and video quality still fail. Training-only raw-logit supervision improves known position to97–100%, not direction; full core gates fail. Frozen probability-to-code probes and measured direction collisions motivate paired updater learning. See docs/modality-readout-plan.md and runs/modality_repair_v1/report.html. No default or green capability promotion. |
| Action proposal | To discuss | Discussed examples of physical/software actions and internal/external distinctions; the concrete action/execution interface has not been walked through. | Connect action-head outputs to typed tool/physical commands, durations, execution outcomes and feedback. |
| Generated-content reflection | Discussed | Discussed reusing modality encoders for recalled/generated material while routing it into focus rather than treating it as a new observation. | Distinguish the implemented generated-output reflection path from proposed raw-memory replay. |

## Green: validated scopes

### Observation adapters

Packet deduplication/order, one action/time advance per event, commit-once and masked-input handling.

Still open: Live webcam capture, cross-device synchronization and real streaming are not validated. Discussion walkthrough still pending.

Evidence: [Belief implementation verification (69 CPU tests)](../runs/belief_v1/verification.json), [Event, masking and timestamp checks](../tests/test_belief.py).

### Session memory + optional World State → §5/13

Exact recent envelopes, bounded hierarchy, source/belief separation and causal reads. The optional World State adds tested transactional identity/provenance/correction mechanics, pinned historical retrieval and exact session/training restart on the supplied-descriptor development task. Audio/video/text and image packets now pass separate and mixed actual WorldSession/thinker paths after correcting a four-versus-eight categorical-code memory mismatch.

Still open: This validates storage and replay mechanics, not general entity recognition, concept induction, calibrated uncertainty, useful learned retrieval or long-horizon recall. The photo recall path still loses accessible detail. Full-agent historical evaluation must restore the corresponding neural session, not just query an old store revision. Real media transport and tiny direct codec fitting do not establish state-conditioned output quality or cross-modal identity.

Evidence: [Belief implementation verification (69 CPU tests)](../runs/belief_v1/verification.json), [Memory bounds, provenance and causal-read checks](../tests/test_belief.py), [Photo recall limitations](../docs/photo-detail-plan.md), [World State: 3293 independent development checks](../runs/world_state_foundation_v1/verification.json), [81 scoped World State and regression tests](../runs/world_state_foundation_v1/focused-tests.xml), [Foundation protocol, failures, repairs and limits](../docs/world-state-foundation-plan.md), [Non-image modality audit: 6075 artifact checks; repair gate failed](../runs/modality_foundation_v1/verification.json), [112 scoped modality, World State and regression tests](../runs/modality_foundation_v1/final-tests.xml).

### Optional bounded planner → §11

Fixed-horizon candidate evaluation, common sampling, caller-state preservation and RNG restoration; supplied key-box search has independent replay.

Still open: This does not validate learned dynamics, general goal achievement or the proposed action DAG. The linked initial key-box integrated capability screen failed.

Evidence: [Belief implementation verification (69 CPU tests)](../runs/belief_v1/verification.json), [Candidate rejection and state preservation](../tests/test_data_and_planning.py), [Independent search replay; integrated screen failed](../runs/key_box_v1/verification.json).

### Generated-content reflection

Re-encoded generated outputs can change workspace while leaving physical belief/history unchanged; generated content cannot enter source evidence.

Still open: Useful self-reflection and quality of generated content are not established by these routing tests.

Evidence: [Belief implementation verification (69 CPU tests)](../runs/belief_v1/verification.json), [Reflection boundary check](../tests/test_belief.py), [Loopback and provenance checks](../tests/test_tasks.py).
