# Development check on original PushT; not the LeWM data protocol.
dataset: configs/datasets/pusht_cchi.yaml
run_dir: runs/pusht_cchi_dev
seed: 3072
train_fraction: 0.9
train_episodes: null
max_steps: 500
epochs: 10
batch_size: 32
workers: 2
encoder_chunk: 16
precision: bf16
lr: 5.0e-05
weight_decay: 0.001
warmup_fraction: 0.01
grad_clip: 1.0
sigreg_weight: 0.09
sigreg_projections: 1024
sigreg_knots: 17
log_every: 10
eval_every: 100
eval_batches: 8
model: {}
