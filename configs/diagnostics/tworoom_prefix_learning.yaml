# Bounded fixed-data learning diagnosis; no paper-performance claim.
dataset: configs/datasets/tworoom_prefix.yaml
run_dir: runs/diagnostics/tworoom_prefix_learning
seed: 3072
train_fraction: 0.9
train_episodes: null
max_steps: 400
epochs: 1000
batch_size: 128
workers: 0
encoder_chunk: 16
precision: bf16
lr: 5.0e-05
weight_decay: 0.001
warmup_fraction: 0.01
grad_clip: 1.0
sigreg_weight: 0.09
sigreg_projections: 1024
sigreg_knots: 17
log_every: 10
eval_every: 100
eval_batches: 4
model: {}
max_seconds: 840
cache_bytes: 600000000
