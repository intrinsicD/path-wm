# Paper-budget architecture/optimizer; explicit episode split/scheduler differences
# are documented in docs/baseline.md. This config is not a benchmark success claim.
dataset: configs/datasets/pusht.yaml
run_dir: runs/pusht_baseline
seed: 3072
train_fraction: 0.9
train_episodes: null
max_steps: null
epochs: 10
batch_size: 128
workers: 4
encoder_chunk: 16
precision: bf16
lr: 0.00005
weight_decay: 0.001
warmup_fraction: 0.01
grad_clip: 1.0
sigreg_weight: 0.09
sigreg_projections: 1024
sigreg_knots: 17
log_every: 10
eval_every: 500
eval_batches: 16
model: {}
