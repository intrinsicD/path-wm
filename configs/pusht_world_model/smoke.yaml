schema_version: 1
task: pusht-eup
seed: 3107
device: cpu
cpu_threads: 2
smoke: true
development_subset: {train_episodes: 2, validation_episodes: 1, prefix: 32}
training:
  learning_rate: 0.0003
  weight_decay: 0.0001
  grad_clip: 1.0
  validate_every: 1
  encoder_batch_size: 32
  early_stopping: false
  perception: {updates: 2, batch_size: 2, validation_examples: 4}
  memory: {updates: 2, batch_size: 1, validation_examples: 1}
  predictor_1: {updates: 2, batch_size: 2, validation_examples: 4}
  predictor_5: {updates: 2, batch_size: 1, validation_examples: 4}
evaluation:
  split: validation
  control_cases: 2
  prediction_windows: 6
  diagnostic_episodes: 2
  control_max_steps: 10
  candidates: 32
  iterations: 2
  elites: 8
  horizon: 5
  controllers: [learned, reset, hold, repeat_last, random, replay]
